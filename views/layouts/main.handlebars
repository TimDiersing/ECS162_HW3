<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">   
    
    <!-- font awesome for the trashcan icon --> 
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">    
    
    <title>{{{appName}}}</title>
    <link rel="icon" type="image/png" ref="public/images/court-logo.png">

    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <header>
        <div class="inner-header">
            <div class="logo">
                <img src="images/court-logo.png" class="header-logo">
            </div>
            <nav>
                <!-- conditional rendering based on variables - Is the user logged in?-->
                {{#if loggedIn}}
                    <a href="/">Home</a>
                    <a href="/profile">Profile</a>
                    <a href="/logout">Logout <span>{{user.username}}</span> </a>
                    <div class="user-info">

                        <!-- conditional rendering based on variables -->
                        <img src="/avatar/{{user.username}}" 
                                    alt="User Avatar" 
                                    class="header-avatar">
                    </div>
                {{else}}
                    <a href="/">Home</a>
                    <a href="/login">Login/Register</a>
                {{/if}}
            </nav>
        </div>
    </header>

    {{{body}}}

    <footer>
        <p>© {{{copyrightYear}}} {{{appName}}}. All rights reserved.</p>
    </footer>
</body>
</html>
